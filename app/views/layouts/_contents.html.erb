<div class="contents">
<ol>
	<% @cms_site.pages.root.children.published.each do |page| %>
	  <li><%= link_to page.label, page.url %>
	    <ol>
	      <% page.children.published.each do |page| %>
	        <li><%= link_to page.label, page.url %></li>
  	      <% end %>
  	    </ol>
  	  </li>
	<% end %>
</ol>
</div>
